{
  "name": "animanga",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "dev": "yarn lerna run --stream dev",
    "build": "yarn lerna run --stream build",
    "start": "yarn workspace @animanga/server start"
  }
}
